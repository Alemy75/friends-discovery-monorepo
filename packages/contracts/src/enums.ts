export enum AccountKind {
  Single = 'single',
  Couple = 'couple',
}

export enum Intent {
  Walks = 'walks',
  DoubleDates = 'double-dates',
  Hobby = 'hobby',
  Travel = 'travel',
  DeepTalks = 'deep-talks',
  Events = 'events',
}
